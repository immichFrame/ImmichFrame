services:
  immichframe:
    build:
      context: ..
      target: dev
    ports:
      - 8080:8080
    env_file:
      - .env
    # volumes:
      # - PATH/TO/CONFIG:/app/Config
      # - PATH/TO/APPDATA:/app/App_Data
